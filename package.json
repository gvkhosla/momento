{
  "name": "momento",
  "version": "0.2.0",
  "description": "Remember everything you heart, bookmark, or share from X.",
  "type": "module",
  "bin": {
    "momento": "./src/cli.js"
  },
  "scripts": {
    "momento": "node src/cli.js",
    "start": "node src/cli.js serve",
    "test": "node --test",
    "check": "for f in src/*.js extension/src/*.js public/*.js; do node --check \"$f\" || exit 1; done"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
